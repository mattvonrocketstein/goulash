[tox]
envlist = py27

#[testenv:docs]
#basepython=python
#changedir=docs/source
#deps =
#    sphinx
#commands =
#    sphinx-build -a -b html -d {envtmpdir}/doctrees . {envdir}/build/html

[testenv]
deps=
     -r{toxinidir}/goulash/tests/requirements.txt
commands=
    python setup.py develop
    py.test --capture=no \
            --cov-config {toxinidir}/.coveragerc \
            --cov=goulash --cov-report=term -v \
            --pyargs {toxinidir}/goulash/tests/{posargs}
setenv=
    FOO=BAR
